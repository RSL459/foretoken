# SPDX-License-Identifier: Apache-2.0
# SPDX-FileCopyrightText: Copyright contributors to the Foretoken project

# Select frontend and model alerts independently. Empty lists keep alerts disabled.
apiVersion: inference.foretoken.io/v1alpha1
kind: FrontendService
metadata:
  name: quickstart-frontend
spec:
  observability:
    alerts:
      rules: [] # Example: [ForetokenMetricsTargetDown]
---
apiVersion: inference.foretoken.io/v1alpha1
kind: ModelService
metadata:
  name: quickstart-qwen3-0.6b
spec:
  observability:
    alerts:
      rules: [] # Example: [ForetokenMetricsTargetDown]
